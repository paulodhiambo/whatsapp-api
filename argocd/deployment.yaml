apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whatsapp-api
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/paulodhiambo/whatsapp-api.git
    targetRevision: main
    path: helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: whatsapp-api
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
